data <- c(23,24,24,25,24,24,24,25,26,22,21)
# Center/location
mean(data)  # average of the data
median(data)  # median of the data
# Dispersion 
sd(data)  # standard deviation of the data
var(data) # variance of the data
IQR(data) # the interquartile range of the data
# Rank 
min(data) # the min of the data
max(data) # the max of the data
quantile(data, 0.25) # the quantile of the data

#setwd()
#quite understandble